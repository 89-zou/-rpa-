# 小邹RPA 使用说明

> 一个跑在 Windows 上的**流程自动化工具**：把「打开网页 / 点按钮 / 填表单 / 采数据 / 循环 / 判断」
> 这些操作画成流程图，点一下就跑；节点里也能直接写 Python / JavaScript。
> 图形化编排 + 真实浏览器（Playwright）+ 桌面截图定位。
>
> 作者：**@小邹**

**目录**： [第一次运行](#一第一次运行) · [界面速览](#二界面速览) · [搭第一条流程](#三搭第一条流程) ·
[节点清单](#四节点清单) · [关键功能](#五关键功能怎么用) · [数据与搬家](#六数据存在哪怎么搬家) ·
[常见问题](#七常见问题)

---

## 一、第一次运行

双击 `小邹RPA.exe`。**如果程序旁边已经有 `projects\` 文件夹**（比如你拿的是别人打包好的整套文件夹），
程序会直接进主界面，不用做下面这些。否则会弹一个窗口：

| 窗口上的东西 | 说明 |
|---|---|
| 一个路径框 +【浏览…】 | **数据放哪儿**。程序会在这个位置建 `projects\`（放你的流程）、`浏览器\`（放浏览器内核） |
| ☑ 创建示例项目（采集示例-登录与采集） | 勾上的话，复制一份内置示例进去，照着它改最快 |
| ☑ 下载浏览器内核 Chromium（约 150 MB，跑网页流程要用） | 只跑桌面自动化可以不勾（以后也能补：重开程序再勾一次） |
| 【开始】 | 开始准备。全程有日志；下载失败可以重试，或先跳过 |

**两个建议**

- 数据目录**别选 C 盘**（内核 + 项目数据都不小），D 盘随便建个文件夹，比如 `D:\小邹RPA`。
- 如果同一个文件夹里**已经有 `projects\` 和 `浏览器\`**，直接把路径指到那儿——**不会重复下载**，
  第一次用别人拷给你的整套目录时特别省事（内核一个就 700 MB）。
- 下载慢是正常的（走的是国内镜像）。实在不行先跳过，等要跑网页流程时再重开程序补下载。

准备完之后，这个文件夹里会有：

```
D:\小邹RPA\
├─ projects\            每个项目一个子文件夹（流程 + 数据 + 图片 + 登录态）
├─ 浏览器\              浏览器内核（Chromium，约 700 MB）
└─ crash.log            出问题时的日志（正常不会有）
```

> 数据目录选过就记住了，存在 `%APPDATA%\小邹RPA\config.json` 里，以后不再问。
> 想换：直接改这个 json 里的 `data_dir`（把这一行删掉＝下次重开又重新问）。

## 二、界面速览

顶部一排按钮：

| 按钮 | 干什么 |
|---|---|
| 【新建项目…】 | 填名称 + 起始网址 + 场景（网页自动化 / 桌面应用），建完立即载入 |
| 【载入项目…】 | 从项目文件夹里挑一个打开（也能【浏览其他文件夹…】） |
| 【项目管理…】 | 一个项目的全部配置：**变量清单 / 图片库 / 登录态 / 采集数据 / 函数库** |
| 【流程编辑…】 | 列表式看整条流程（带缩进），**结构改动都在这里做**；改动即时保存，只有【关闭】 |
| 【自动排版】 | 按横向蛇形把画布上的节点重新排整齐（超出宽度自动换行） |
| 【运行】/【停止】 | 跑 / 停。跑的时候右下角弹出运行小窗 |
| 【继续（人工处理完）】/【终止（暂停中）】 | 流程停在「暂停等人工」节点时才亮 |
| ﹀ 日志 / ︿ 日志 | 收起 / 展开主界面下半部分的日志面板 |

**画布**（中间那块）

- 拖动卡片＝摆位置；**Ctrl + 滚轮**＝缩放（缩放跟着项目记住，下次打开还是这个大小）
- **双击空白处**＝在那个位置加节点；**双击卡片**＝编辑它
- **右键空白处**＝【新增步骤】；**右键卡片**＝编辑 / 在此之前插入 / 删除 / 上移 / 下移
- 循环和条件会套一层**虚线框**（循环紫、条件蓝）。**拖虚线框＝整块一起搬**，拖单张卡片就只动那一张
- 块里的虚线框可以一层套一层（条件里放循环、循环里放条件都行）

**运行小窗**（点【运行】后右下角出现）

标题会在 `▶ 正在运行` / `⏸ 已暂停` / `⏸ 等人工处理` / `■ 正在停止` 之间变；按钮有【暂停】/【继续】/【终止】/【显示主页】。
小窗上会写「第几步、在干什么」，以及最近几行日志。

## 三、搭第一条流程

以「登录网站 → 采一页内容」为例：

1. **新建**：点【新建项目…】，名称写「我的采集」，起始网址填要打开的网址，场景选「网页自动化」。
2. **先打开网页**：起始网址会自动生成第 1 个节点「打开网页」。
3. **点登录**：双击空白处 → 选「点击」→ 定位那一栏点【捕获元素…】，
   程序弹出浏览器，点一下页面上的「登录」按钮，XPath 自动填进来。
4. **填表单**：同法加两个「填入」节点，值里写账号密码（写死的、或者写 `{{账号}}` 这种变量）。
5. **等页面反应**：在「点击」节点的「步骤后等待」里选**等待元素出现**，目标填登录后才有的元素
   （比如退出按钮）——这样点完不会抢跑。
6. **采数据**：加「采集数据」节点，选「列表模式」，行定位用【捕获元素…】点页面上任意一行，
   再加几个字段（标题 / 链接 / 内容…）。
7. **跑**：点【运行】。跑完在【项目管理…】→【采集数据】里看记录，能【导出 Excel…】/【导出 CSV…】。

> 不确定某个节点怎么填？每个对话框里都有一个 **?** 按钮，点开就是这个节点的用法说明。

## 四、节点清单

| 分组 | 节点 | 说明 |
|---|---|---|
| **网页** | 打开网页 | 输入网址；站点慢就把超时调大 |
| | 点击 | 点元素；可设点几次（2＝双击）；可配「截图兜底」 |
| | 填入 | 先点一下拿到焦点再输入，填完会**读回校验**，日志里写校验结果 |
| | 下拉选择 | `<select>` 下拉框；填 option 的 value 或显示文字 |
| | 采集数据 | 当前页采一条 / 页面上多行每行一条；字段支持 文字 / 属性 / 这段 HTML / 链接 / 图片 / 附件 / 截图 |
| | 暂停等人工 | 停下来等验证码之类，满足恢复条件或人工点【继续】 |
| **桌面** | 激活窗口 | 把目标程序窗口切到最前面（桌面场景专用） |
| | 按键 | 快捷键，如 `enter`、`ctrl+s`、`alt+f4` |
| | 等待 | 纯等几秒 |
| **数据** | 读取数据 | 读文件夹 / 单个文本 / Excel / JSON，产出一个列表变量，交给「循环」逐项处理 |
| **结构** | 循环 | 见下面「循环」一节 |
| | 条件 if/else | 见下面「条件」一节 |
| | 组合 | 把连着的一串步骤收成一张卡片（画面清爽，也方便挂条件规则） |
| **代码** | 自由代码 | 写一个真正的 Python / JavaScript 函数 |
| | 调用函数 | 调用【项目管理…】→【函数库】里定义好的函数 |
| **其它** | 提示 / 日志 | 画布上的一句话说明；运行时把内容打进日志（不碰浏览器，最安全） |

## 五、关键功能怎么用

### 1. 定位元素（XPath）

- **【捕获元素…】** 是主力：弹出浏览器 → 点页面上的元素 → XPath 自动填好，
  还会问你要不要**存成一条「元素定位」**。存了以后任何地方都能写 `{{那条的名字}}` 引用，
  以后网站改了路径，改一处、全项目跟着变。
- 定位框里**只能填 XPath**，说明文字写到「备注」那一栏（编辑器会拦）。
- 点不动、页面结构老变？给定位配一张**兜底截图**：同框里点【捕获元素…】生成一张，
  运行时 XPath 找不到就改用截图匹配再试一次。
- 桌面应用没有 XPath：直接用截图定位 + 系统鼠标键盘。

### 2. 步骤后等待

每个动作节点都能设「这一步做完后再等什么」：

| 选项 | 什么时候用 |
|---|---|
| 等待元素出现（推荐） | 点完/填完等某个元素出来再往下——**能扛住整页跳转** |
| 等待页面跳转 / 加载完成 | 这一下会跳新页面 |
| 等待 URL 变化 | 只是地址栏变了 |
| 等待网络空闲 | 页面是慢慢加载出来的 |
| 手动 | 不自动等（配合「暂停等人工」用） |

另外还有一个「再固定等几秒」，用来兜底那些说不清的情况。

### 3. 变量

步骤里写 `{{变量名}}` 就能引用。变量的来源有五种：

1. **变量清单**（【项目管理…】→【变量清单】里手工加的，如 `账号` / `密码`）
2. **元素定位**（捕获元素时存的 XPath，和变量一样用 `{{名字}}` 引用）
3. **节点产出**（「读取数据」产出列表、「采集数据」产出字段、「自由代码」里 `@名字 = 值` 写回的）
4. **循环注入**（循环里天然有 `{{loop.item}}` / `{{loop.index}}`）
5. 数据源读出来的每个字段（勾选过的才会产出）

跑之前程序会**检查一遍**：引用了没有来源的变量会列出来问你要不要继续。

### 4. 循环

循环节点有两种方式，选错方式是最容易踩的坑：

**方式一：次数 / 列表**（for —— 先算好一份清单，跑完就结束）

「循环内容」里填：

| 填什么 | 意思 |
|---|---|
| `10` | 跑 10 次，`{{loop.item}}` 是当前索引 0~9 |
| `{{文章列表}}` | 按这个变量的**项数**逐项跑（列表 / JSON 数组 / 多行文本都行） |
| 一段文字 | 按行 / 逗号切开，每项跑一次 |

**方式二：条件**（while —— 每轮开头先判断一次，判断不出次数）

「判断什么」有四个选项：**网页上有这个元素**（填 XPath）/ **屏幕上找到这张图**（选项目里的图片）/
**变量满足条件**（变量 + 判断方式 + 值）/ **自定义表达式**（Python，能用 `元素存在(...)`、`图片存在(...)`）。

「成立时」决定语义：

- **继续下一轮** → 条件是 while：成立就一直跑
- **结束循环** → 条件是 until：**一直等到某样东西出现**（监控场景就用这个，比如「一直等到『完成.png』出现」）

防死循环有两道闸：**每轮间隔**（默认 1 秒，监控场景一定要填）+ **最多轮数**（默认 10000，0＝不限）；
加上【停止】按钮就是三道。

循环体里能直接用 `{{loop.item}}`（当前项；读出来的是对象时，字段写成 `{{loop.item.标题}}`）
和 `{{loop.index}}`（第几轮，从 1 开始）。

### 5. 条件（if / else）

**条件节点只负责给出「判断的数据」**，真正比什么由条件体里每个节点自己的规则决定：

- 条件节点上只填一样：判断的数据（如 `{{loop.item.标题}}`，或者一段 Python 表达式）
- 条件体里摆的每个节点，各自带一条规则：**判断方式 + 值**
  - 判断方式：包含 / 不包含 / 等于 / 不等于 / 大于 / 小于 / 大于等于 / 小于等于
  - 值可以写多个，**逗号分隔＝命中任意一个就算成立**（如 `申报, 项目, 通知`）
- **从上往下比，第一个成立的执行**——所以节点的先后就是优先级
- **兜底**：判断方式留空＝无条件成立。想让「上面都不成立时也有地方去」，就把它放**最后一个**

举例：标题里有「公示」就点「公示公告」，有「申报」就点「申报通知」，都没有就点「公示公告」——

```
条件（数据：{{loop.item.标题}}）
  ├─ 点击 公示公告   规则〔包含 公示〕
  ├─ 点击 申报通知   规则〔包含 申报〕
  └─ 点击 公示公告   规则〔兜底〕
```

**一个动作要好几步怎么办？** 一个节点只能挂一条规则。先选中那几步 → 右键【合并选中节点…】，
规则就挂在组合上（画布上只显示一张卡片）。

判断特别复杂、一句话说不完的，把条件的判断方式切成「表达式」，直接写 Python。

### 6. 组合

把连着的一串步骤收成一张卡片：**【流程编辑…】里选中那几行 → 右键 →【合并选中节点…】**。
取消组合也在同一个右键菜单（**只拆壳、不删步骤**）。

组合可以标成「**登录用**」：带着有效登录态跑的时候，整块跳过（比如登录那一串步骤）。

### 7. 自由代码 / 函数库

代码框里写一个**完整的函数**，系统自动调用它：

```python
def 处理标题(后缀='（已处理）'):
    标题 = @原始标题.strip()          # 读变量清单（变量名带点时必须写 @，如 @loop.item.标题）
    @结果 = 标题 + 后缀               # 等号左边＝写回变量清单
    @计数 = str(int(@计数 or 0) + 1)  # 循环里累加会跨轮保留
    /封面 = 'D:/图片/封面.png'        # 等号左边＝存回图片库
    log('处理完：' + @结果)            # 打日志
    return @结果
```

- `@名字` 读写变量清单、`/图片名` 读写图片库
- `#名字=路径` 写在**参数表**里，函数里用「名字」拿到的就是那个文件路径（路径能写 `{{变量}}`）
- 还能用 `page`（浏览器页面对象）、`current_url`、`project_dir`、`log()`
- 变量里存的都是**文本**，要算数先 `int()` / `float()`
- 保存后自动进**函数库**，别处用「调用函数」节点复用；改了函数名，调用它的节点自动跟着改
- 执行有超时刹车（写死的循环会被中断）

### 8. 采集与导出

「采集数据」节点两种模式：

- **当前页**：页面上只有一条（比如详情页），采一条
- **列表**：页面上是一列（搜索结果、商品列表），**填「每行的定位」**，每行采一条

字段类型：文字 / 属性 / 这段 HTML / 链接 / 图片（下载到项目 `data/files/`，变量里存文件名）/
附件 / 截图（截这个元素、整页、或指定区域）。

看结果：**【项目管理…】→【采集数据】**，能【导出 Excel…】（表头冻结、列宽撑开）或【导出 CSV…】（中文不乱码）。

### 9. 登录态（不用每次重新登）

- 第一次跑：正常走登录步骤 → **跑完自动把 cookie 存进项目 `auth\` 目录**
- 以后跑：先做一次体检（看「登录后才有的元素」在不在，在【项目管理…】→【登录态】里配）
  - 还在＝登录态有效 → 标了「登录用」的组合**整块跳过**
  - 不在＝失效了 → 自动重跑一遍完整体验
- 想重新看一遍完整登录：把项目 `auth\` 里的 json 删掉再跑

### 10. 暂停等人工（验证码）

「暂停等人工」节点会在跑到那里时停下来，弹出提示语。恢复方式：

| 恢复条件 | 说明 |
|---|---|
| 仅人工继续 | 你处理完点【继续】 |
| 元素出现 | 页面上出现某个元素就自动继续 |
| URL 变化 | 地址栏变成某个样子就自动继续 |
| URL + 元素 | 两个都满足 |

还有「最多等几秒」，超时就报错停下。

## 六、数据存在哪 / 怎么搬家

一个项目就是一个文件夹：

```
projects\我的采集\
├─ steps.json     流程本身（节点、坐标、变量都在里面）
├─ img\           截图（兜底定位、捕获元素存下来的）
├─ data\          读进来的数据、采集结果 records.jsonl、下载的图片附件
└─ auth\          登录态 cookie
```

**搬家/备份**：把**整个数据目录**（或整个项目文件夹）拷到别的电脑 / U 盘就行，接着跑。
数据源里写死的文件路径找不到时，程序会**按文件名在项目目录里自动找同名文件**，日志里会写一句。

## 七、常见问题

| 现象 | 怎么办 |
|---|---|
| 提示「还没装浏览器内核」 | 重开程序，在准备环境的窗口里勾上「下载浏览器内核 Chromium」再点【开始】 |
| 不想让内核占 C 盘 | 默认就不在 C 盘：内核装在 `<数据目录>\浏览器\`。把数据目录设成别处即可 |
| 元素点不到 | 先用【捕获元素…】重抓 XPath；页面结构会变的，给定位配一张兜底截图 |
| 程序闪退、没有任何提示 | 打开数据目录里的 `crash.log`，里面有出错时的调用栈 |
| 卡在某个循环里 | 用【停止】；给循环设「最多轮数」；自由代码的循环有超时刹车，到点会中断 |
| 想换数据目录 | 改 `%APPDATA%\小邹RPA\config.json` 里的 `data_dir`；把这一行删掉＝下次重开重新问 |
| 换了电脑后提示「文件不存在」 | 一般不用改：会按文件名在项目目录里自动找。找不到就重新选一次那个文件夹 |
| 界面卡住不动 | 看运行小窗：可能停在「⏸ 等人工处理」上，处理完点【继续】 |
| 想跑某个项目但不开界面 | 命令行：`.venv\Scripts\python run_cli.py 项目名`（源码运行时用） |
