# Python 缓存
__pycache__/
*.py[cod]

# 项目内虚拟环境（体积大，含 PyQt6 二进制，不入库）
.venv/

# IDE
.idea/

# 崩溃日志
crash.log
*.log

# 用户自己的项目数据与截图：
#   projects/<项目名>/steps.json 里可能存着账号密码，
#   而且这些内容是你平时在界面里编辑的，入库会把历史弄得很难看。
#   如果你确实想连流程配置一起做版本管理，把下面这行删掉即可（注意密码会进 git）。
projects/

# 临时验证脚本目录（跑完就删）
projects/_verify_*/
