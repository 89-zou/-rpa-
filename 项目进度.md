# smart_tool 项目进度与约定

> 每次开工先读本文件；本轮结束请更新「进度」与「待办」两节。
> 最后更新：2026-09-17（对话第 N 轮，详见文末"变更记录"）

---

## 一、这是什么

独立的「智能化工具」：**浏览器自动化 + AI 写文章**。项目路径 `d:\桌面程序\流程\smart_tool`。

- 入口：`smart_tool/main.py`（GUI，PyQt6）；`run_cli.py`（命令行跑流程）
- 环境：项目本地 `.venv`（Python 3.13），**禁止向全局 Python 装包**
- 依赖：PyQt6 / playwright / opencv-python / openpyxl / xlrd（见 `requirements.txt`）
- 数据：`projects/<项目名>/steps.json` + `projects/<项目名>/img/`（截图）
- 崩溃日志：`crash.log`（`main.py` 里装了全局异常钩子，会弹窗+写文件）

## 二、核心概念（改代码前必须知道）

| 概念 | 说明 |
|---|---|
| 步骤 Step | `core/project_store.py`，id 从 1 连续编号（流程编辑里每次改动都会重排） |
| 动作 action | `navigate` / `click` / `fill` / `select` / `pause_for_human` / `loop_start` / `loop_end` / `script` |
| 变量 | 步骤里用 `{{变量名}}` 引用；来源＝项目变量 + 数据源产出 + 循环注入 |
| 数据源 | `core/data_sources.py`，四种类型 excel/json/txt/folder |
| `field_map` | **"这个数据源要保存哪些变量"的清单**（可改名），四种类型通用；空清单＋`vars_picked=True` = 一个都不产出 |
| `vars_picked` | 没挑过变量 → 产出全部原始变量（兼容旧项目）；挑过 → 只产出清单里的 |
| 循环 | `loop_start`~`loop_end` 之间的步骤重复执行；**不支持嵌套** |
| 循环来源 | `loop_source`: `data`（数据源每行）/ `list`（变量或手动列表，见 `loop_items`）/ `range`（数字范围，见 `loop_range`） |
| 循环范围 | `range` 来源时填 `loop_range`：`10`=跑 10 次（1~10）、`5-10`=5~10 共 6 次、`{{次数}}`、`{{起}}-{{止}}`、`1-{{止}}`；**起止都含**，起始值不填按 1 算 |
| 循环变量 | `{{loop.item}}` 当前项（range 来源＝当前数字）、`{{loop.index}}` 序号(1起)、`{{loop.zero_index}}`；列表项是对象时其键按 `{{row.键}}` 注入 |
| 等待方式 | `wait_after`: 空 / `element_present` / `page_load` / `url_changed` / `network_idle` / `manual` |
| 人工暂停 | 恢复条件 `manual` / `url_changed` / `element_present` / `url_and_element` |

## 三、已实现（本阶段全部完成并验证）

**浏览器自动化**
- XPath / 截图（OpenCV 多尺度匹配）两种定位
- `fill` 先点击元素中心获取焦点再填入；**填完读回校验**，日志显示 `填入校验通过：N 个字符（目标元素 id=xxx）`
- 步骤后等待全部为**轮询实现，能扛整页跳转/刷新**：元素、URL、页面加载完成、网络空闲
- 人工暂停：URL 与 DOM 双信号自动恢复 + 人工继续/终止 + 超时
- 循环：数据源逐行 / 变量·手动列表 / **数字范围（次数）**，三种来源
  - 数字范围：框里写 `10` 就是跑 10 次（数字 1~10）；`5-10` 跑 5~10 共 6 次；起止都能用变量（`{{次数}}`、`{{起}}-{{止}}`、`1-{{止}}`）
  - 范围框右侧有「插入变量 ▾」下拉（和 fill 一样），点一下把变量插到光标处
  - 起止都填字面量且起 > 止时，编辑器直接拦下；运行期遇到同样情况跳过循环体并写日志
- 自由代码节点：Python（本地 exec）/ JavaScript（页面内执行），可配置传入变量
- 运行前**变量检查**：引用了没有来源的变量会列出来并询问是否继续

**数据源**
- excel/json/txt/folder 四种；可读文件内容、文件名、父文件夹名、路径、扩展名、大小等
- 读取预览后**勾选要保存的变量 + 自由改名**；未勾选的不产出
- 预览显示每个变量的实际取值与字数（确认"正文取全了没有"）

**界面**
- 顶部：`新建项目…` / `载入项目…` / `变量管理…`；**启动不自动载入项目**（不阻塞 GUI）
- 画布：Coze 风格自由拖拽、横向蛇形自动排版、Ctrl+滚轮缩放、日志 ︿/﹀ 折叠
- 流程编辑：列表式编排，**改动即时保存 + 编号从 1 重排**，只有【关闭】
- 变量管理：数据源变量（只读、标注来源）与项目变量（可改），**增删改即时保存**；数据源变量删除会同步改数据源配置
- 步骤编辑器：按动作/定位方式/等待方式/恢复条件**整行（含标签）显隐**；保存时校验（XPath 语法、定位框混说明文字、循环项为空等）

## 四、踩过的坑（别再犯）

1. **Qt 槽函数里抛异常 → PyQt6 直接 abort 整个进程**（表现为无提示闪退）。
   - 已加全局异常钩子（`main.py`），任何异常都会写 `crash.log` + 弹窗
   - 真实案例：双击节点编辑 → 保存触发画布重建 → 删掉正在处理事件的节点 → 返回再调基类崩溃。修法：**先把事件交回基类，再延后一拍发信号**（`flow_canvas.py` `mouseDoubleClickEvent`）
   - `_rebuild_scene` 必须**先丢 Python 引用再 `scene.clear()`**
2. **`page.url` 在 `time.sleep` 期间是过期缓存**，暂停轮询必须 `page.evaluate("() => location.href")`
3. 定位/等待字段**只能填 XPath**，说明文字要写到备注（编辑器已拦）
4. 数据源 `_apply_field_map` 的 `field` 既接受短名（`content`）也接受全名（`file.content`），统一走 `resolve_src_key()`
5. 改 HTML 表单定位时注意：WordPress 登录页的 input 是写在 `<label>` **内部**的（`//p/label/input[@id="user_login"]` 有效）

## 五、当前项目数据

`projects/宏创为`（用户的实际项目，**改代码时不要动**；截至本轮共 7 步）：

1. 打开登录页 `https://www.hcwgx.com/wp-login.php?loggedout=true`
2. fill 密码 → `//p/label/input[@id="user_pass"]`，值 `{{密码}}`
3. fill 账号 → `//p/label/input[@id="user_login"]`，值 `{{账号}}`
4. click 登录按钮 → 等待元素 `//*[@id="menu-posts"]/a/div[3]`
5. click 「文章」菜单 → `//*[@id="menu-posts"]/a/div[3]`
6. loop_start（来源＝数据源，**数据源还没配**）
7. loop_end（循环体目前是空的）

变量：`账号=hcw`、`密码=hcw20182019`

## 六、待办

- [ ] **第 5 步 AI 助手**（尚未开始）：smolagents + litellm 接入；集成 `policy-wechat-article` 技能自动写文章；lxml 做 HTML 规范自检
  - 依赖 `smolagents`/`litellm`/`lxml` 在 `.venv` 里**都还没装**（Python 3.13，装前先确认版本兼容）
  - `policy-wechat-article` 技能不在本项目，位于 `C:\Users\Mr.zou\Desktop\公众号文章生成\.trae\skills\`；它靠宿主的 WebFetch/WebSearch 抓链接，搬进来要改写成项目内纯 Python 抓取
- [ ] 循环嵌套（暂不支持，遇到需求再评估）
- [ ] 数据源"文件夹数量"类需求：目前用脚本节点产列表 + 循环来源选「变量」实现；**单纯"跑 N 次"现在直接用「数字范围」来源**

## 七、验证与清理约定

- 测试脚本放 `projects/_verify_<n>/`，用 `QT_QPA_PLATFORM=offscreen` 跑，**跑完删掉**
- 每条命令：`.venv\Scripts\python.exe -u -m projects._verify_v9.run_verify`
- 每次改完至少跑：本轮验证 + 一条跨功能回归（画布/变量/循环/等待别互相踩）
- 收尾要 `compileall` 编译检查 + 确认 `projects/` 下只剩用户自己的项目

## 八、变更记录（新→旧）

- **本轮**：循环新增「数字范围」来源（`loop_range`：`10`／`5-10`／`{{变量}}`／`{{起}}-{{止}}`，起止都含；范围框带「插入变量 ▾」下拉）；数字范围循环里的 `row.*` 引用会在运行前提示；编辑器与画布摘要、循环框文案同步支持
- **上轮**：循环支持变量/手动列表（`loop_source`/`loop_items`）；等待改为轮询并新增「等待页面跳转/加载完成」；定位框混说明文字的拦截；点击跳转类报错修复；填入读回校验；闪退根因修复 + `crash.log` 机制；流程编辑即时保存+编号重排；fill 先聚焦再填
- **上上轮**：变量管理（数据源变量可见可管、全可删、即时保存）；【载入项目…】替代下拉刷新；数据源"勾选要保存的变量 + 改名"；无关选项整行隐藏；新建项目移出到顶部
- **更早**：截图定位、人工暂停双信号、可视化编排（列表弹窗+自由画布）、循环结构、脚本节点、数据源四类型、字段自定义变量名、旧格式迁移、节点缩放、日志折叠、画布视野修复
